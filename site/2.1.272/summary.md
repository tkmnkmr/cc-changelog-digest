Claude Code v2.1.272、Monitor監視に期限が付き無期限待機ができなくなった。

1. 自動モードのBash/PowerShell/Monitorが必要なホストだけを都度レビューして開放するようになった
2. Monitor監視は最大30分（-p実行時10分）で必ず期限切れになり、無期限待機は廃止された
3. v2.1.272は詳細非公開の軽微な修正のみ。破壊的変更・新モデルはv2.1.271分のみ該当

今日やること
- Monitorで無期限監視（persistentオプション）に頼っていた人 → 期限切れ後に自動で再起動される前提に運用を見直す
- 自動モードでBash/PowerShell/Monitorを使っている人 → ドメイン許可が都度レビューに変わったことを確認する
